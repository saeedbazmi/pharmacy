# task — M2: مقایسه و ری‌دایرکت

معیار پذیرش هر آیتم در `plan.md` کنار همین فایل است؛ این فایل فهرست کار اجرایی است.

نشانه‌ها: `[ ]` انجام‌نشده، `[~]` در جریان، `[x]` تمام. نقش: **B** = `backend-dev`، **F** = `frontend-dev`.

---

## PH1-016 — داروخانه دوم · B

- [ ] 016.1 ذخیره نمونه پاسخ منبع دوم در `testdata/` مستقل
- [ ] 016.2 پیاده‌سازی `Fetcher` دوم به‌عنوان **یک فایل جدید**، بدون تغییر هیچ فایل هسته‌ای
- [ ] 016.3 افزودن ردیف `data_sources` برای منبع دوم (بدون کد سخت‌شده)
- [ ] 016.4 تست ایزوله‌سازی خطا: منبع دوم عمداً خراب، منبع اول باید سالم بماند
- [ ] 016.5 اجرای تطبیق روی داده دوم و بررسی تعداد کالاهای مشترک متصل‌شده
- [ ] 016.6 تست Fetcher دوم روی `testdata` بدون تماس شبکه

## PH1-014 — جدول قیمت داروخانه‌ها · B + F

- [ ] 014.1 (B) تابع خالص `rankByPrice` با تعیین offer دارای کم‌ترین قیمت + تست جدولی
- [ ] 014.2 (B) حذف offer با وضعیت `suspicious` از پاسخ عمومی
- [ ] 014.3 (B) افزودن نشانگر «کم‌ترین قیمت» به پاسخ API (تصمیم در بک‌اند، نه فرانت)
- [ ] 014.4 (F) کامپوننت `OfferTable` با `<table>` واقعی و `<th scope>`
- [ ] 014.5 (F) بج «کم‌ترین قیمت» با توکن `--color-best-price`
- [ ] 014.6 (F) نمایش موجودی با متن + آیکن (نه فقط رنگ)
- [ ] 014.7 (F) نمایش زمان آخرین به‌روزرسانی هر offer
- [ ] 014.8 (F) دکمه خرید با توکن `--color-accent` (تنها کاربرد این رنگ)
- [ ] 014.9 (F) حالت خالی با پیام فارسی و پیشنهاد جای‌گزین

## PH1-017 — مسیر ری‌دایرکت خرید · B + F

- [ ] 017.1 (B) مایگریشن `redirect_clicks` با پارتیشن‌بندی ماهانه بر اساس زمان
- [ ] 017.2 (B) ایجاد خودکار پارتیشن ماه آینده (job زمان‌بندی‌شده)
- [ ] 017.3 (B) هندلر `GET /go/{offerId}`: خواندن offer، ثبت غیرهم‌زمان کلیک، `302`
- [ ] 017.4 (B) ری‌دایرکت فقط به `offers.product_url` — تست ضد open redirect
- [ ] 017.5 (B) ثبت `pharmacy_id`، `offer_id`، `product_id`، زمان، منبع ارجاع
- [ ] 017.6 (B) تضمین اینکه کندی ثبت کلیک ری‌دایرکت را معطل نمی‌کند (بافر/غیرهم‌زمان با سقف)
- [ ] 017.7 (B) لاگ `redirect.click` و پاسخ فارسی برای `offerId` نامعتبر
- [ ] 017.8 (F) دکمه خرید به‌صورت `<a href="/go/{offerId}" rel="nofollow sponsored">` بدون `onClick`

## PH1-015 — فیلتر و مرتب‌سازی · B + F

- [ ] 015.1 (B) پشتیبانی پارامترهای `sort`، `in_stock`، `brand`، `pharmacy` در API
- [ ] 015.2 (B) اعتبارسنجی پارامتر و بازگشت به مقدار پیش‌فرض در ورودی نامعتبر
- [ ] 015.3 (B) بازبینی ایندکس کوئری فیلتر با `EXPLAIN`
- [ ] 015.4 (F) خواندن `searchParams` در Server Component و ساخت لینک‌های فیلتر
- [ ] 015.5 (F) فرم فیلتر با ارسال سروری تا بدون جاوااسکریپت هم کار کند
- [ ] 015.6 (F) حفظ وضعیت فیلتر پس از رفرش و قابل اشتراک بودن لینک

## PH1-018 — مقایسه چند کالا · F

- [ ] 018.1 کامپوننت انتخاب کالا برای مقایسه (کلاینتی و فقط برگ درخت)
- [ ] 018.2 نگه‌داشتن انتخاب‌ها در URL برای اشتراک‌پذیری
- [ ] 018.3 صفحه/جدول مقایسه با کم‌ترین قیمت و تعداد داروخانه هر کالا
- [ ] 018.4 سقف تعداد کالای قابل مقایسه و پیام هنگام عبور از سقف
- [ ] 018.5 بازبینی: صفحه مقایسه بودجه JS را نقض نمی‌کند

## PH1-019 — شفافیت تازگی داده · F

- [ ] 019.1 نمایش زمان به‌روزرسانی به شکل خوانا («۱۲ دقیقه پیش»)
- [ ] 019.2 نشان هشدار با `--color-warning` برای قیمت کهنه‌تر از آستانه
- [ ] 019.3 متن توضیح کنار جدول: خرید و قیمت نهایی در سایت داروخانه
- [ ] 019.4 بازبینی دسترس‌پذیری جدول قیمت: پیمایش کیبوردی و `focus-visible`

---

## ترتیب پیشنهادی

016 → 014 → 017 → 015 → 018 → 019.

## بازبینی پایانی

- [ ] همه معیارهای پذیرش `plan.md` تیک خورده‌اند
- [ ] دو منبع فعال با `sync_runs` سالم
- [ ] `redirect_clicks` داده واقعی دارد و در `docs/metrics.md` ثبت شده است
- [ ] بودجه LCP و حجم JS صفحه کالا نقض نشده است
- [ ] هیچ آیتمی از جنس سبد خرید/پرداخت وارد نشده (خواسته‌های جدید در `docs/backlog.md`)
