# task — M2: مقایسه و ری‌دایرکت

معیار پذیرش هر آیتم در `plan.md` کنار همین فایل است؛ این فایل فهرست کار اجرایی است.

نشانه‌ها: `[ ]` انجام‌نشده، `[~]` در جریان، `[x]` تمام. نقش: **B** = `backend-dev`، **F** = `frontend-dev`.

---

## PH1-016 — داروخانه دوم · B

- [x] 016.1 ذخیره نمونه پاسخ منبع دوم در `testdata/` مستقل
- [x] 016.2 پیاده‌سازی `Fetcher` دوم به‌عنوان **یک فایل جدید**، بدون تغییر هیچ فایل هسته‌ای
- [x] 016.3 افزودن ردیف `data_sources` برای منبع دوم (بدون کد سخت‌شده)
- [x] 016.4 تست ایزوله‌سازی خطا: منبع دوم عمداً خراب، منبع اول باید سالم بماند
- [x] 016.5 اجرای تطبیق روی داده دوم و بررسی تعداد کالاهای مشترک متصل‌شده
- [x] 016.6 تست Fetcher دوم روی `testdata` بدون تماس شبکه

## PH1-014 — جدول قیمت داروخانه‌ها · B + F

- [x] 014.1 (B) تابع خالص `rankByPrice` با تعیین offer دارای کم‌ترین قیمت + تست جدولی
- [x] 014.2 (B) حذف offer با وضعیت `suspicious` از پاسخ عمومی
- [x] 014.3 (B) افزودن نشانگر «کم‌ترین قیمت» به پاسخ API (تصمیم در بک‌اند، نه فرانت)
- [x] 014.4 (F) کامپوننت `OfferTable` با `<table>` واقعی و `<th scope>`
- [x] 014.5 (F) بج «کم‌ترین قیمت» با توکن `--color-best-price`
- [x] 014.6 (F) نمایش موجودی با متن + آیکن (نه فقط رنگ)
- [x] 014.7 (F) نمایش زمان آخرین به‌روزرسانی هر offer
- [x] 014.8 (F) دکمه خرید با توکن `--color-accent` (تنها کاربرد این رنگ)
- [x] 014.9 (F) حالت خالی با پیام فارسی و پیشنهاد جای‌گزین

## PH1-017 — مسیر ری‌دایرکت خرید · B + F

- [x] 017.1 (B) مایگریشن `redirect_clicks` با پارتیشن‌بندی ماهانه بر اساس زمان
- [x] 017.2 (B) ایجاد خودکار پارتیشن ماه آینده (job زمان‌بندی‌شده)
- [x] 017.3 (B) هندلر `GET /go/{offerId}`: خواندن offer، ثبت غیرهم‌زمان کلیک، `302`
- [x] 017.4 (B) ری‌دایرکت فقط به `offers.product_url` — تست ضد open redirect
- [x] 017.5 (B) ثبت `pharmacy_id`، `offer_id`، `product_id`، زمان، منبع ارجاع
- [x] 017.6 (B) تضمین اینکه کندی ثبت کلیک ری‌دایرکت را معطل نمی‌کند (بافر/غیرهم‌زمان با سقف)
- [x] 017.7 (B) لاگ `redirect.click` و پاسخ فارسی برای `offerId` نامعتبر
- [x] 017.8 (F) دکمه خرید به‌صورت `<a href="/go/{offerId}" rel="nofollow sponsored">` بدون `onClick`

## PH1-015 — فیلتر و مرتب‌سازی · B + F

- [x] 015.1 (B) پشتیبانی پارامترهای `sort`، `in_stock`، `brand`، `pharmacy` در API
- [x] 015.2 (B) اعتبارسنجی پارامتر و بازگشت به مقدار پیش‌فرض در ورودی نامعتبر
- [x] 015.3 (B) بازبینی ایندکس کوئری فیلتر با `EXPLAIN`
- [x] 015.4 (F) خواندن `searchParams` در Server Component و ساخت لینک‌های فیلتر
- [x] 015.5 (F) فرم فیلتر با ارسال سروری تا بدون جاوااسکریپت هم کار کند
- [x] 015.6 (F) حفظ وضعیت فیلتر پس از رفرش و قابل اشتراک بودن لینک

## PH1-018 — مقایسه چند کالا · F

- [x] 018.1 کامپوننت انتخاب کالا برای مقایسه (کلاینتی و فقط برگ درخت)
- [x] 018.2 نگه‌داشتن انتخاب‌ها در URL برای اشتراک‌پذیری
- [x] 018.3 صفحه/جدول مقایسه با کم‌ترین قیمت و تعداد داروخانه هر کالا
- [x] 018.4 سقف تعداد کالای قابل مقایسه و پیام هنگام عبور از سقف
- [x] 018.5 بازبینی: صفحه مقایسه بودجه JS را نقض نمی‌کند

## PH1-019 — شفافیت تازگی داده · F

- [x] 019.1 نمایش زمان به‌روزرسانی به شکل خوانا («۱۲ دقیقه پیش»)
- [x] 019.2 نشان هشدار با `--color-warning` برای قیمت کهنه‌تر از آستانه
- [x] 019.3 متن توضیح کنار جدول: خرید و قیمت نهایی در سایت داروخانه
- [x] 019.4 بازبینی دسترس‌پذیری جدول قیمت: پیمایش کیبوردی و `focus-visible`

---

## ترتیب پیشنهادی

016 → 014 → 017 → 015 → 018 → 019.

## بازبینی پایانی

- [x] همه معیارهای پذیرش `plan.md` تیک خورده‌اند
- [x] دو منبع فعال با `sync_runs` سالم
- [x] `redirect_clicks` داده واقعی دارد و در `docs/metrics.md` ثبت شده است
- [x] بودجه LCP و حجم JS صفحه کالا نقض نشده است
- [x] هیچ آیتمی از جنس سبد خرید/پرداخت وارد نشده (خواسته‌های جدید در `docs/backlog.md`)
