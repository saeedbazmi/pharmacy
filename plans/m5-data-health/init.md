# init — M5: سلامت داده و اعتماد

این فایل نقطه‌ی ورود به Milestone است. کامل بخوانش، سپس `plan.md` کنار آن را.

## ترتیب مطالعه

1. `AGENT.md` — بخش ۵ (ایمنی قیمت و `sync_run`) و ۶ (لاگ و مشاهده‌پذیری).
2. `features.md` — «پایش سلامت همگام‌سازی» و «گزارش کلیک‌های ری‌دایرکت».
3. `plans/m5-data-health/plan.md`.

## Skill فعال

- `backend-dev` برای تشخیص جهش، پایداری کرال، کوئری گزارش‌ها.
- `frontend-dev` برای صفحات سلامت، صف بازبینی و گزارش کلیک در پنل.

## وضعیت موردانتظار ریپو

- [x] دروازه خروج M4 کامل است.
- [x] `sync_runs` و `redirect_clicks` داده‌ی واقعی چندروزه دارند (بدون داده‌ی واقعی، آستانه‌ها قابل کالیبره نیستند).
- [x] پنل با نقش `data_ops` کار می‌کند و audit فعال است.

## راه‌اندازی

```bash
make up
make migrate    # وضعیت suspicious روی offers + ایندکس‌های گزارش
make worker
```

پیش از تعیین آستانه‌ها، توزیع واقعی تغییرات قیمت را از `price_history` استخراج کن. آستانه‌ی حدسی یا صف بازبینی را فلج می‌کند یا قیمت غلط را رد می‌کند.

## ترتیب اجرای پیشنهادی

PH1-034 → PH1-035 → PH1-033 → PH1-037 → PH1-036 → PH1-038.

دلیل: جلوگیری از انتشار قیمت غلط فوری‌ترین کار است؛ پایش و گزارش پس از آن.

## تصمیم‌های باز

- آستانه‌ی جهش قیمت (پیش‌فرض ±۷۰٪) پس از دیدن داده‌ی واقعی چه عددی باشد؟
- آستانه‌ی «کهنه» و آستانه‌ی «بحرانی» چند ساعت است؟
- offer کهنه از مقایسه حذف شود یا با هشدار بماند؟ این یک تصمیم محصولی است، نه فنی — نیازمند تایید کاربر.

## دروازه خروج

- [x] جهش قیمت آزمایشی به سایت نمی‌رسد و در صف بازبینی می‌نشیند.
- [x] صفحه سلامت وضعیت واقعی همه منابع و آخرین خطا را نشان می‌دهد.
- [x] منبع عمداً خراب، بقیه‌ی منابع را متوقف نمی‌کند.
- [x] گزارش کلیک به تفکیک داروخانه با خروجی CSV کار می‌کند.
- [x] آستانه‌های انتخاب‌شده با دلیل در `docs/decisions/` ثبت شده‌اند.

## بعدی

`plans/m6-optional-accounts/init.md`
