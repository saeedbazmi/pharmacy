# task — M4: پنل تیم داده

معیار پذیرش هر آیتم در `plan.md` کنار همین فایل است؛ این فایل فهرست کار اجرایی است.

نشانه‌ها: `[ ]` انجام‌نشده، `[~]` در جریان، `[x]` تمام. نقش: **B** = `backend-dev`، **F** = `frontend-dev`.

---

## PH1-026 — احراز هویت داخلی و نقش‌ها · B + F

- [ ] 026.1 (B) مایگریشن `internal_users` با نقش (`data_ops`, `super_admin`) و وضعیت فعال
- [ ] 026.2 (B) ذخیره گذرواژه با الگوریتم هش قوی و کند (بدون هش ساده)
- [ ] 026.3 (B) endpoint ورود و خروج با سشن کوکی `HttpOnly` + `Secure` + `SameSite=Lax`
- [ ] 026.4 (B) محدودیت نرخ تلاش ورود به‌ازای کاربر و IP
- [ ] 026.5 (B) middleware بررسی نقش روی همه مسیرهای `/api/v1/ops/...` (پیش‌فرض بستن، نه بازکردن)
- [ ] 026.6 (B) تست: کاربر بدون نقش و کاربر با نقش اشتباه `403` می‌گیرند
- [ ] 026.7 (F) گروه مسیر `app/(admin)` با چیدمان جدا و `robots: noindex`
- [ ] 026.8 (F) صفحه ورود پنل و هدایت کاربر بدون دسترسی
- [ ] 026.9 (B) لاگ `auth.login`، `auth.failed`، `auth.rate_limited` بدون داده حساس

## PH1-031 — ثبت audit · B

- [ ] 031.1 مایگریشن `audit_logs` با actor، موجودیت، عمل، مقدار قبل/بعد، زمان
- [ ] 031.2 helper مشترک ثبت audit در سرویس‌ها (نه در handler)
- [ ] 031.3 ماسک/حذف فیلدهای حساس (کلید API) پیش از ذخیره
- [ ] 031.4 کوئری فهرست audit با فیلتر کاربر و بازه زمانی + ایندکس لازم
- [ ] 031.5 تست: هر عملیات نوشتنی پنل یک ردیف audit می‌سازد

## PH1-027 — مدیریت منبع داده · B + F

- [ ] 027.1 (B) CRUD داروخانه: ساخت، ویرایش، غیرفعال‌سازی
- [ ] 027.2 (B) CRUD منبع داده با نوع (`api`/`crawler`) و اعتبارسنجی شکل `config JSONB`
- [ ] 027.3 (B) اعتبارسنجی URL منبع: فقط `http`/`https` و مسدودسازی IP داخلی (ضد SSRF)
- [ ] 027.4 (B) ذخیره کلید API به‌صورت فقط‌نوشتنی؛ در خروجی همیشه ماسک‌شده
- [ ] 027.5 (B) endpoint «تست اتصال» که چند آیتم نمونه را بدون ذخیره برمی‌گرداند
- [ ] 027.6 (B) تنظیم و اعتبارسنجی بازه زمان‌بندی سینک
- [ ] 027.7 (F) صفحه فهرست منابع با وضعیت آخرین اجرا
- [ ] 027.8 (F) فرم ساخت/ویرایش منبع با اعتبارسنجی سمت کلاینت و سرور
- [ ] 027.9 (F) نمایش نتیجه «تست اتصال» به‌صورت جدول پیش‌نمایش

## PH1-028 — صف تطبیق کالا · B + F

- [ ] 028.1 (B) کوئری فهرست `match_candidates` با وضعیت `pending` مرتب بر امتیاز
- [ ] 028.2 (B) عملیات تایید در یک ترنزکشن: اتصال `source_item` به `product` و انتقال offer
- [ ] 028.3 (B) عملیات رد و عملیات «اتصال به کالای دیگر» با جست‌وجوی کالا
- [ ] 028.4 (B) ساخت کالای جدید از یک `source_item`
- [ ] 028.5 (B) قابلیت بازگردانی تطبیق تاییدشده
- [ ] 028.6 (B) لاگ `match.approved` و `match.rejected` با شناسه اپراتور
- [ ] 028.7 (F) صفحه صف تطبیق با نمایش داده خام منبع و کالای پیشنهادی کنار هم
- [ ] 028.8 (F) کنترل‌های تایید/رد/اتصال دستی با تایید عملیات مخرب
- [ ] 028.9 (F) صفحه‌بندی و فیلتر صف بر اساس منبع و امتیاز

## PH1-030 — تایید و مخفی‌کردن کالا · B + F

- [ ] 030.1 (B) افزودن وضعیت `published`/`hidden`/`needs_review` و اعمال آن در همه کوئری‌های عمومی
- [ ] 030.2 (B) ویرایش دستی مشخصات کالا با نگه‌داشتن مقدار اصلی منبع
- [ ] 030.3 (B) قفل فیلد ویرایش‌شده تا سینک بعدی آن را بازنویسی نکند
- [ ] 030.4 (B) تست: کالای `hidden` در API عمومی، جست‌وجو و sitemap نمی‌آید
- [ ] 030.5 (F) صفحه ویرایش کالا با نمایش هم‌زمان مقدار منبع و مقدار دستی
- [ ] 030.6 (F) کنترل تغییر وضعیت با پیام تایید

## PH1-029 — مدیریت دسته‌بندی و برند · B + F

- [ ] 029.1 (B) CRUD دسته‌بندی درختی با جابه‌جایی و غیرفعال‌سازی
- [ ] 029.2 (B) مسدودسازی حذف دسته‌بندی دارای کالا یا الزام به انتقال کالاها
- [ ] 029.3 (B) حفظ slug قدیم یا ری‌دایرکت هنگام تغییر slug دسته‌بندی
- [ ] 029.4 (B) CRUD برند و عملیات ادغام برندهای تکراری در یک ترنزکشن
- [ ] 029.5 (F) صفحه مدیریت درخت دسته‌بندی
- [ ] 029.6 (F) صفحه مدیریت و ادغام برند

## PH1-032 — اجرای دستی سینک · B + F

- [ ] 032.1 (B) endpoint ساخت job سینک برای یک منبع
- [ ] 032.2 (B) جلوگیری از انبار شدن job تکراری با کلیک مکرر
- [ ] 032.3 (B) endpoint وضعیت job/آخرین اجرا
- [ ] 032.4 (F) دکمه «سینک همین حالا» با نمایش وضعیت در حال اجرا و نتیجه

---

## ترتیب پیشنهادی

026 → 031 → 027 → 028 → 030 → 029 → 032. احراز هویت و audit باید پیش از هر صفحه تغییردهنده داده وجود داشته باشند.

## بازبینی پایانی

- [ ] همه معیارهای پذیرش `plan.md` تیک خورده‌اند
- [ ] یک نفر غیرمهندس با راهنمای کوتاه یک منبع جدید را متصل و سینک می‌کند
- [ ] حداقل ۵۰ تطبیق واقعی از پنل تایید شده است
- [ ] هیچ مسیر `/ops` بدون بررسی نقش نمانده و هیچ کلید API خوانده نمی‌شود
- [ ] `docs/metrics.md` شامل تعداد `pending` صف تطبیق است
