# init — M0: اسکلت و زیرساخت

این فایل نقطه‌ی ورود به Milestone است. کامل بخوانش، سپس `plan.md` کنار آن را.

## ترتیب مطالعه

1. `AGENT.md` — بخش ۲ (استک)، ۳ (ساختار پوشه)، ۴ (مدل داده)، ۶ (لاگ).
2. `plans/m0-foundation/plan.md` — آیتم‌ها و معیارهای پذیرش.
3. `features.md` — فقط برای درک تصویر کلی؛ در این Milestone هیچ فیچر کاربری ساخته نمی‌شود.

## Skill فعال

- `backend-dev` برای PH1-001 تا PH1-006.
- `frontend-dev` برای PH1-007 و PH1-008.

## وضعیت موردانتظار ریپو

پیش‌نیاز: ریپو خالی است و فقط `AGENT.md`، `features.md` و `plans/` وجود دارند. این اولین Milestone است، پس هیچ پیش‌نیاز کدی ندارد.

نیازمندی محیط: Docker، Go 1.23+، Node 20+.

## راه‌اندازی

```bash
cp deploy/.env.example .env
make up
make migrate
```

## ترتیب اجرای پیشنهادی

PH1-001 → PH1-004 → PH1-005 → PH1-003 → PH1-002 → PH1-006، و به‌صورت موازی PH1-007 → PH1-008.

دلیل: دیتابیس و config پیش از سرور لازم است؛ فرانت به بک‌اند وابسته نیست و می‌تواند هم‌زمان جلو برود.

## تصمیم‌های باز

- نام دامنه و محیط استقرار (برای تنظیم Caddy در M7 لازم می‌شود، الان فقط جای‌نگه‌دار).
- اولین داروخانه‌ی هدف برای M1 — بهتر است همین حالا مشخص شود تا M1 بدون معطلی شروع شود.

## دروازه خروج

تا این شرط‌ها برآورده نشده‌اند، M1 شروع نمی‌شود:

- [x] `make up` و `make migrate` بدون خطا اجرا می‌شوند.
- [x] `/healthz` و `/readyz` پاسخ درست می‌دهند.
- [x] لاگ JSON با `request_id` تولید می‌شود.
- [x] صفحه‌ی فرانت با فونت فارسی و جهت راست‌به‌چپ بالا می‌آید.
- [x] اسکیمای هسته مایگریت شده و ایندکس‌ها موجودند.

## بعدی

`plans/m1-single-pharmacy-pipeline/init.md`
