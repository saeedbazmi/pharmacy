# task — M6: حساب کاربری اختیاری

معیار پذیرش هر آیتم در `plan.md` کنار همین فایل است؛ این فایل فهرست کار اجرایی است.

نشانه‌ها: `[ ]` انجام‌نشده، `[~]` در جریان، `[x]` تمام. نقش: **B** = `backend-dev`، **F** = `frontend-dev`.

---

## PH1-039 — ورود با OTP · B

- [ ] 039.1 مایگریشن `users` و `otp_codes` (کد هش‌شده، انقضا، مصرف‌شده، شماره تلاش)
- [ ] 039.2 اعتبارسنجی و نرمال‌سازی شماره موبایل ایران پیش از ارسال
- [ ] 039.3 اینترفیس `SMSSender` + پیاده‌سازی واقعی و پیاده‌سازی جعلی برای محیط توسعه (پشت فلگ محیط)
- [ ] 039.4 endpoint درخواست کد: تولید کد، ذخیره هش‌شده، ارسال
- [ ] 039.5 endpoint تایید کد: مقایسه با زمان ثابت، بررسی انقضا، باطل‌کردن پس از یک بار مصرف
- [ ] 039.6 محدودیت نرخ به‌ازای شماره و به‌ازای IP با پیام فارسی روشن
- [ ] 039.7 ماسک شماره در لاگ و اطمینان از اینکه کد OTP هرگز لاگ نمی‌شود
- [ ] 039.8 لاگ `otp.sent`، `otp.verified`، `otp.rate_limited`
- [ ] 039.9 تست: کد منقضی، کد مصرف‌شده، کد اشتباه، عبور از سقف نرخ

## PH1-040 — سشن کاربر · B

- [ ] 040.1 ساخت سشن با کوکی `HttpOnly` + `Secure` + `SameSite=Lax` و انقضای مشخص
- [ ] 040.2 middleware شناسایی کاربر و افزودن `actor_id` به `context` و لاگ
- [ ] 040.3 endpoint خروج با باطل‌کردن سشن
- [ ] 040.4 تست: نقش `user` به مسیرهای `/ops` و `/admin` دسترسی ندارد
- [ ] 040.5 بازبینی: همه قابلیت‌های هسته برای کاربر مهمان بدون ورود کار می‌کند

## PH1-044 — رابط کاربری حساب · F

- [ ] 044.1 صفحه ورود با ورودی OTP (`type="tel"`، `inputMode="numeric"`، `autoComplete="one-time-code"`)
- [ ] 044.2 شمارش معکوس ارسال مجدد و نمایش پیام محدودیت نرخ
- [ ] 044.3 غیرفعال‌کردن دکمه در زمان ارسال با `useFormStatus`
- [ ] 044.4 صفحه حساب کاربری با بخش علاقه‌مندی‌ها، هشدارها و خروج
- [ ] 044.5 بازبینی: کامپوننت‌های تعاملی فقط برگ درخت‌اند و صفحات عمومی کلاینتی نشده‌اند
- [ ] 044.6 دسترس‌پذیری فرم: `<label>` متصل، پیام خطای قابل خواندن، `focus-visible`

## PH1-041 — لیست علاقه‌مندی · B + F

- [ ] 041.1 (B) مایگریشن `favorites` با یکتایی `(user_id, product_id)`
- [ ] 041.2 (B) افزودن/حذف idempotent و کوئری فهرست با کم‌ترین قیمت فعلی هر کالا (بدون N+1)
- [ ] 041.3 (F) Server Action افزودن/حذف علاقه‌مندی
- [ ] 041.4 (F) جریان «کاربر مهمان روی افزودن کلیک کند»: پیشنهاد ورود و ذخیره همان کالا پس از ورود
- [ ] 041.5 (F) صفحه علاقه‌مندی‌ها با حالت خالی و پیشنهاد جست‌وجو

## PH1-042 — ثبت هشدار قیمت و موجودی · B + F

- [ ] 042.1 (B) مایگریشن `price_alerts` با نوع هشدار، قیمت هدف اختیاری، وضعیت
- [ ] 042.2 (B) اعمال سقف تعداد هشدار فعال هر کاربر
- [ ] 042.3 (B) endpointهای ثبت، فهرست و حذف هشدار
- [ ] 042.4 (F) کنترل ثبت هشدار در صفحه کالا با تعیین قیمت هدف
- [ ] 042.5 (F) صفحه مدیریت هشدارها با حذف و حالت خالی

## PH1-043 — ارزیابی و ارسال هشدار · B

- [ ] 043.1 job دوره‌ای ارزیابی هشدارها به‌صورت دسته‌ای (بدون N+1)
- [ ] 043.2 شرط ارسال: offer نه `suspicious` و نه کهنه‌تر از آستانه
- [ ] 043.3 جلوگیری از اعلان تکراری برای یک رویداد (کلید یکتای ارسال)
- [ ] 043.4 backoff و سقف تلاش در شکست ارسال؛ هشدار حذف نشود
- [ ] 043.5 لاگ `alert.triggered` و `alert.sent` بدون شماره کامل
- [ ] 043.6 تست: افت قیمت آزمایشی یک اعلان و فقط یک اعلان تولید کند

## PH1-045 — تاریخچه جست‌وجو (مشروط) · B + F

- [ ] 045.1 (B) اتصال `search_queries` به `user_id` برای کاربر وارد‌شده
- [ ] 045.2 (B) endpoint فهرست و پاک‌کردن تاریخچه
- [ ] 045.3 (F) بخش تاریخچه در حساب کاربری با دکمه پاک‌کردن
- [ ] 045.4 این آیتم فقط پس از اتمام PH1-039 تا PH1-044 و در صورت باقی‌ماندن وقت انجام می‌شود

---

## ترتیب پیشنهادی

039 → 040 → 044 → 041 → 042 → 043 → 045 (مشروط).

## بازبینی پایانی

- [ ] همه معیارهای پذیرش `plan.md` تیک خورده‌اند
- [ ] هیچ کد OTP یا شماره کامل در لاگ نیست
- [ ] کاربر مهمان هیچ قابلیت هسته‌ای از دست نداده است
- [ ] اعلان افت قیمت یک بار و فقط برای قیمت معتبر ارسال می‌شود
- [ ] هیچ آیتمی از جنس پروفایل، نظرات یا سبد خرید وارد نشده است
