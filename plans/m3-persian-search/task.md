# task — M3: جست‌وجوی فارسی

معیار پذیرش هر آیتم در `plan.md` کنار همین فایل است؛ این فایل فهرست کار اجرایی است.

نشانه‌ها: `[ ]` انجام‌نشده، `[~]` در جریان، `[x]` تمام. نقش: **B** = `backend-dev`، **F** = `frontend-dev`.

---

## PH1-021 — نرمال‌سازی پرسش کاربر · B + F

- [x] 021.1 (B) استفاده از **همان** `platform/textfa` خط داده برای پرسش کاربر (بدون پیاده‌سازی دوم)
- [x] 021.2 (B) حذف فاصله اضافی و نویسه‌های کنترلی از پرسش
- [x] 021.3 (B) رد پرسش خالی یا بیش از حد طولانی با پیام فارسی (نه ۵۰۰)
- [x] 021.4 (B) تست جدولی: «استامینوفن» / «استامينوفن» / «استا مینوفن» / «acetaminophen» هم‌ارز شوند
- [x] 021.5 (F) نرمال‌سازی ورودی در `lib/persian.ts` پیش از ارسال

## PH1-020 — SearchPort و آداپتر Postgres · B

- [x] 020.1 تعریف اینترفیس `SearchPort` در `module/search` (سرویس فقط به آن وابسته باشد)
- [x] 020.2 مایگریشن فعال‌سازی `pg_trgm` و `unaccent`
- [x] 020.3 مایگریشن ستون/ویو نرمال‌شده جست‌وجو + ایندکس GIN trigram
- [x] 020.4 پر کردن ستون نرمال‌شده برای داده موجود و به‌روزرسانی آن در مسیر ingest
- [x] 020.5 کوئری جست‌وجو روی نام فارسی، نام لاتین، برند و ماده مؤثره
- [x] 020.6 رتبه‌بندی: تطابق دقیق → شروع با عبارت → شباهت trigram
- [x] 020.7 کالیبره کردن آستانه شباهت با پرسش‌های واقعی و ثبت عدد در `plan.md`
- [x] 020.8 حذف کالای `hidden` و حذف‌شده نرم از نتایج
- [x] 020.9 آداپتر Postgres پشت `SearchPort` (بدون وابستگی مستقیم سرویس به SQL)

## PH1-022 — endpoint جست‌وجو · B

- [x] 022.1 `GET /api/v1/products?q=...` با صفحه‌بندی سروری و سقف اندازه صفحه
- [x] 022.2 پشتیبانی فیلترهای برند، دسته‌بندی و «فقط موجودها»
- [x] 022.3 افزودن کم‌ترین قیمت و تعداد داروخانه هر کالا به پاسخ (بدون N+1)
- [x] 022.4 کش پاسخ با کلید شامل پرسش نرمال‌شده و فیلترها
- [x] 022.5 اعتبارسنجی و پیش‌فرض‌گذاری همه پارامترها

## PH1-023 — صفحه نتایج جست‌وجو · F

- [x] 023.1 مسیر `app/(public)/search/page.tsx` به‌صورت Server Component با `searchParams`
- [x] 023.2 کامپوننت کارت نتیجه: نام، برند، کم‌ترین قیمت، تعداد داروخانه، موجودی با متن
- [x] 023.3 نوار فیلتر و مرتب‌سازی متصل به URL
- [x] 023.4 صفحه‌بندی با `<a>` واقعی برای پیمایش موتور جست‌وجو
- [x] 023.5 حالت بدون نتیجه با پیشنهاد اصلاح املا یا مرور دسته‌بندی
- [x] 023.6 ورودی جست‌وجو با `<label>` متصل، قابل استفاده با کیبورد
- [x] 023.7 `loading.tsx` با skeleton هم‌ابعاد و `error.tsx` فارسی

## PH1-024 — لاگ و سنجش جست‌وجو · B

- [x] 024.1 مایگریشن `search_queries` با پرسش نرمال‌شده، تعداد نتیجه، `duration_ms`، زمان
- [x] 024.2 ثبت غیرهم‌زمان جست‌وجو بدون اثر محسوس روی زمان پاسخ
- [x] 024.3 لاگ `search.query` با `result_count` و `duration_ms`
- [x] 024.4 کوئری گزارش «جست‌وجوهای بی‌نتیجه» و «پرتکرارترین‌ها»
- [x] 024.5 ایندکس لازم برای گزارش‌های زمانی

## PH1-025 — تثبیت کارایی · B

- [x] 025.1 تولید داده حجیم واقع‌نما (حداقل چند ده هزار کالا)
- [x] 025.2 `EXPLAIN ANALYZE` کوئری جست‌وجو و اطمینان از استفاده از ایندکس (بدون seq scan)
- [x] 025.3 اندازه‌گیری صدک ۹۵ زمان پاسخ زیر بار سبک
- [x] 025.4 ثبت اعداد در `docs/metrics.md`
- [x] 025.5 در صورت عبور از ۲۰۰ms: ثبت تصمیم (بهینه‌سازی ایندکس یا مهاجرت به Meilisearch) در `docs/decisions/`

---

## ترتیب پیشنهادی

021 → 020 → 022 → 023 → 024 → 025. نرمال‌سازی پیش‌نیاز ایندکس است؛ ایندکس روی متن نرمال‌نشده باید دوباره ساخته شود.

## بازبینی پایانی

- [x] همه معیارهای پذیرش `plan.md` تیک خورده‌اند
- [x] سرویس جست‌وجو فقط به `SearchPort` وابسته است
- [x] `search_queries` پر می‌شود و نرخ بی‌نتیجه در `docs/metrics.md` ثبت شده است
- [x] عدد صدک ۹۵ اندازه‌گیری و مستند شده است
