دارو/محصول از طریق API داروخانه‌ها یا کرال کردن سایت‌شان لیست می‌شود.
کاربر جستجو می‌کند، قیمت‌ها را در چند داروخانه مقایسه می‌کند و روی «خرید از فلان داروخانه» کلیک می‌کند → ری‌دایرکت به سایت اصلی داروخانه.

سوال اول: گروه‌های کاربری (بازبینی‌شده)
الف. کاربران مهمان (Guest Users)
دسترسی‌ها: جستجو و مرور محصولات، مشاهده جزئیات کالا، مشاهده و مقایسه قیمت همان کالا در چند داروخانه، فیلتر/مرتب‌سازی، کلیک روی «خرید» و ری‌دایرکت به سایت داروخانه مبدا. هدف پلتفرم: تبدیل بازدیدکننده به کاربر ثبت‌نام‌کرده فقط در صورت نیاز به ویژگی‌های شخصی‌سازی‌شده (علاقه‌مندی‌ها، هشدار قیمت) 

ب. کاربران ثبت‌نام‌کرده (اختیاری، فقط برای شخصی‌سازی)
دسترسی‌ها: ورود با OTP، مدیریت لیست علاقه‌مندی‌ها، فعال‌سازی هشدار کاهش قیمت/موجود شدن کالا، مشاهده تاریخچه جستجو.

ج. تیم محتوا و داده (Content & Data Ops) یا همان تامین‌کنندگان/فروشندگان و پزشک و داروساز فقط منبع داده اند. دسترسی‌ها:
تعریف و مدیریت منبع داده هر داروخانه (اتصال API یا کرالر اختصاصی)
تطبیق/یکپارچه‌سازی کالاهای مشابه از منابع مختلف زیر یک کالای واحد (Product Matching)
پایش سلامت همگام‌سازی قیمت و موجودی (خطای کرال، قطعی API)
مدیریت دسته‌بندی‌ها و برندها
تایید یا مخفی‌کردن کالاهای دارای اطلاعات نادرست/ناقص
مشاهده گزارش کلیک‌های ری‌دایرکت به تفکیک داروخانه
د. مدیر کل (Super Admin)
دسترسی‌ها: مدیریت کاربران داخلی و سطح دسترسی تیم محتوا، تنظیمات عمومی سایت (دسته‌بندی، بنر، سئو)، داشبورد کلی آمار (ترافیک، جستجوهای پرتکرار، نرخ کلیک ری‌دایرکت)، فعال‌سازی قابلیت «خرید مستقیم» برای یک داروخانه منتخب در آینده (Feature Flag فاز دوم).

سوال دوم: یوزکیس‌ها
الف. یوزکیس‌های کاربر مهمان
جستجو و مرور دسته‌بندی‌ها — جستجو بر اساس نام کالا، برند یا ترکیب دارویی؛ مرور از طریق منوی دسته‌بندی.
مشاهده صفحه کالا با فهرست قیمت داروخانه‌ها (هسته اصلی محصول) — تصویر، مشخصات، و لیست «این کالا در فلان داروخانه‌ها با این قیمت‌ها موجود است».
فیلتر و مرتب‌سازی نتایج — بر اساس قیمت، برند، ارزان‌ترین/گران‌ترین.
مقایسه چند کالا با هم — انتخاب چند کالای هم‌رده و دیدن جدول مقایسه‌ای.
کلیک «خرید در [نام داروخانه]» → ری‌دایرکت — انتقال کاربر به صفحه همان کالا در سایت داروخانه مبدا + ثبت لاگ کلیک برای آمار (بدون سبد خرید یا پرداخت داخلی).
ثبت‌نام سریع با OTP — فقط وقتی کاربر بخواهد علاقه‌مندی ذخیره کند یا هشدار قیمت بگیرد.

ب. یوزکیس‌های کاربر ثبت‌نام‌کرده (اختیاری)
مدیریت لیست علاقه‌مندی‌ها (Wishlist)
هشدار کاهش قیمت یا موجود شدن مجدد کالا
مشاهده تاریخچه جستجو (nice-to-have، در صورت وقت کافی)

ج. یوزکیس‌های تیم محتوا و داده
افزودن/مدیریت منبع داده هر داروخانه — اتصال API یا تعریف کرالر اختصاصی برای سایت‌هایی که API ندارند.
تطبیق کالاها (Product Matching) — نگاشت کالاهای مشابه از منابع مختلف به یک رکورد کالای واحد تا مقایسه قیمت درست نمایش داده شود.
پایش سلامت همگام‌سازی — بررسی خطای کرال یا قطعی API و به‌روزرسانی قیمت/موجودی.
مدیریت دسته‌بندی و برند.
تایید یا مخفی‌کردن دستی کالاهای دارای داده نادرست.
گزارش کلیک‌های ری‌دایرکت به تفکیک داروخانه — ورودی اصلی برای تصمیم انتخاب داروخانه پایلوت فاز دوم.

د. یوزکیس‌های مدیر کل
مدیریت کاربران داخلی و سطوح دسترسی تیم محتوا.
تنظیمات عمومی سایت.
داشبورد آمار کلی (ترافیک، جستجوهای پرتکرار، نرخ کلیک ری‌دایرکت).
فعال‌سازی «خرید مستقیم» برای یک داروخانه منتخب (فاز دوم).

سوال سوم: تمایز رقابتی (نسخه فاز اول)
در این فاز، تمایز واقعی و قابل‌ساخت فقط همین است: جمع‌آوری قیمت از چند داروخانه (از طریق API یا کرال) + مقایسه صادقانه قیمت + ری‌دایرکت مستقیم به داروخانه، بدون واسطه شدن در فروش. 