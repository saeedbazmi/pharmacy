# AGENT.md — پلتفرم مقایسه قیمت دارو

این فایل سند مرجع معماری، استک و قواعد توسعه‌ی پروژه است. هر Agent یا توسعه‌دهنده‌ای که روی این ریپو کار می‌کند، **قبل از نوشتن کد** باید این فایل را بخواند و تصمیم‌هایش را با آن هم‌راستا کند. شرح دقیق نیازمندی‌های محصول در `features.md` است؛ این فایل «چگونه ساختن» را توضیح می‌دهد.

---

## ۱. خلاصه محصول

یک موتور جست‌وجو و مقایسه قیمت دارو/محصولات سلامت. داده از داروخانه‌ها (API یا کرال سایت) جمع می‌شود، کالاهای مشابه زیر یک رکورد واحد یکپارچه (Product Matching) می‌شوند، کاربر قیمت‌ها را مقایسه می‌کند و با کلیک روی «خرید»، به سایت همان داروخانه ری‌دایرکت می‌شود.

**در فاز اول سبد خرید، پرداخت و سفارش وجود ندارد.** پلتفرم واسطه فروش نیست؛ فقط مقایسه + ری‌دایرکت + ثبت لاگ کلیک.

سه ویژگی که کیفیت محصول را تعیین می‌کند و معماری باید حولشان بهینه شود:

1. **سرعت و کیفیت جست‌وجوی فارسی** (تحمل غلط املایی، نیم‌فاصله، ی/ک عربی).
2. **تازگی و درستی قیمت/موجودی** (خط داده‌ی قابل پایش، نه کرالر یک‌بارمصرف).
3. **SEO** — ترافیک اصلی این نوع محصول از گوگل می‌آید، پس رندر سمت سرور و داده ساختاریافته اجباری است.

---

## ۲. استک انتخابی و دلیل انتخاب

اصل حاکم: **حداقل وابستگی، حداقل سرویس در حال اجرا، حداکثر سرعت** — بدون بستن راه scale در آینده.

| لایه | انتخاب | چرا |
|---|---|---|
| Backend | **Go 1.23+** با `net/http` استاندارد (روتینگ بومی `1.22+`) | تک‌فایل باینری، بدون runtime، مصرف حافظه پایین، concurrency طبیعی برای کرال هم‌زمان چند داروخانه |
| دسترسی به DB | **pgx + sqlc** (بدون ORM) | کوئری SQL خام + کد تایپ‌سیف تولیدشده؛ نه لایه جادویی، نه N+1 پنهان |
| Database | **PostgreSQL 16+** | تک منبع حقیقت؛ هم رابطه‌ای، هم JSONB برای داده خام منابع، هم صف کار، هم جست‌وجوی فاز اول |
| جست‌وجو | فاز ۱: **Postgres + `pg_trgm` + unaccent** / فاز ۲: **Meilisearch** | فاز ۱ بدون سرویس اضافه راه می‌افتد؛ Meilisearch یک باینری Rust است و پشت اینترفیس `SearchPort` بدون تغییر دامنه جایگزین می‌شود |
| صف کار (کرال/سینک) | **جدول jobs در Postgres با `FOR UPDATE SKIP LOCKED`** | حذف کامل وابستگی به Redis/RabbitMQ در فاز ۱؛ ترنزکشنال و قابل مشاهده با SQL |
| کش | فاز ۱: کش درون‌حافظه‌ای Go (TTL) / فاز ۲: **Redis** | تا وقتی یک نود داریم، Redis هزینه اضافه است |
| Frontend | **Next.js 15 (App Router) + TypeScript** | SSR/ISR برای SEO، RSC برای JS کم سمت کلاینت |
| استایل | **Tailwind CSS v4** + کامپوننت‌های کپی‌شده (سبک shadcn) | صفر وابستگی runtime برای UI؛ هیچ UI-Kit سنگینی نصب نمی‌شود |
| فونت | **Vazirmatn** (self-host، subset فارسی، `woff2`) | بدون درخواست به CDN خارجی |
| لاگ | **`log/slog`** خروجی JSON روی stdout | استاندارد کتابخانه، بدون وابستگی |
| متریک | `/metrics` با Prometheus client (اختیاری، پشت فلگ) | فقط اگر لازم شد فعال شود |
| Trace | **OpenTelemetry** فقط در لایه HTTP/DB (اختیاری) | برای فاز رشد؛ در فاز ۱ خاموش |
| اجرا | **Docker Compose + Caddy** | TLS خودکار، سه کانتینر: `api`، `web`، `db` (+ `worker`) |

**چیزهایی که عمداً استفاده نمی‌کنیم:** ORM (GORM/Prisma)، Kubernetes در فاز ۱، Kafka، GraphQL، Elasticsearch، microservices، MongoDB، هر UI-Kit سنگین، هر state-manager سمت کلاینت (Redux/MobX).

### چرا مونولیت مدولار و نه میکروسرویس؟

مرزها **در کد** رعایت می‌شوند (هر module فقط از طریق interface با بقیه حرف می‌زند)، اما دیپلوی یکپارچه است. اگر روزی کرالر یا جست‌وجو بار زیادی آورد، همان module بدون بازنویسی دامنه به سرویس مستقل تبدیل می‌شود. `cmd/worker` از همین ابتدا جدا از `cmd/api` اجرا می‌شود تا این جداسازی عملی باقی بماند.

---

## ۳. ساختار پوشه‌ها

```
pharmacy/
├─ AGENT.md
├─ features.md
├─ docker-compose.yml
├─ Makefile
├─ backend/
│  ├─ cmd/
│  │  ├─ api/         # سرور HTTP عمومی + پنل
│  │  ├─ worker/      # زمان‌بند + اجراکننده jobs (کرال، سینک، هشدار قیمت)
│  │  └─ migrate/     # اجرای مایگریشن‌ها
│  ├─ internal/
│  │  ├─ platform/    # زیرساخت: config, postgres, logger, httpx, jobq, cache, otel
│  │  ├─ http/        # router, middleware, DTO، مپینگ خطا (بدون منطق دامنه)
│  │  └─ module/
│  │     ├─ catalog/    # کالا، برند، دسته‌بندی
│  │     ├─ pricing/    # offer (قیمت هر کالا در هر داروخانه) + تاریخچه قیمت
│  │     ├─ pharmacy/   # داروخانه و منبع داده‌اش (API/Crawler) + سلامت سینک
│  │     ├─ ingest/     # fetcher، normalizer، product matching
│  │     ├─ search/     # SearchPort + آداپتر postgres و meili
│  │     ├─ identity/   # OTP، کاربر، نقش، دسترسی
│  │     ├─ alerting/   # wishlist و هشدار قیمت/موجودی
│  │     └─ analytics/  # کلیک ری‌دایرکت، جست‌وجوهای پرتکرار، داشبورد
│  ├─ db/migrations/   # SQL خام، شماره‌گذاری‌شده، فقط forward
│  └─ db/queries/      # ورودی sqlc
├─ web/
│  ├─ app/             # مسیرهای عمومی + (admin)
│  ├─ components/
│  ├─ lib/             # کلاینت API، فرمت‌کننده‌ها، نرمال‌سازی فارسی
│  └─ styles/
└─ deploy/             # Caddyfile، فایل‌های env نمونه
```

هر module داخلی این الگو را دارد:

```
module/<name>/
├─ service.go     # منطق کسب‌وکار (تنها جایی که قاعده دامنه دارد)
├─ repository.go  # اینترفیس + پیاده‌سازی Postgres
├─ model.go       # تایپ‌های دامنه (بدون تگ json)
└─ handler.go     # ترنسپورت HTTP (نازک، فقط اعتبارسنجی و مپینگ)
```

**قاعده‌ی وابستگی:** `handler → service → repository`. هیچ‌وقت برعکس. یک module هرگز repository ماژول دیگر را import نمی‌کند؛ فقط interface سرویسش را.

---

## ۴. مدل داده (هسته)

جداکردن «کالای واحد» از «آگهی هر داروخانه» مهم‌ترین تصمیم داده‌ای پروژه است:

- `products` — کالای یکپارچه (نام فارسی/لاتین، ماده مؤثره، شکل دارویی، دوز، برند، دسته، تصویر، `slug`، `status`).
- `brands`, `categories` — دسته‌بندی درختی سبک.
- `pharmacies` — داروخانه (نام، لوگو، دامنه، وضعیت).
- `data_sources` — به هر داروخانه یک منبع: `type` (`api` | `crawler`)، `config JSONB`، `schedule`، وضعیت آخرین اجرا.
- `source_items` — **داده خام هر منبع همان‌طور که آمده** (`raw JSONB`, `external_id`, `fetched_at`). هرگز پاک نمی‌شود؛ مبنای بازپردازش و دیباگ.
- `offers` — قیمت و موجودی: `(product_id, pharmacy_id)` یکتا + `price`, `in_stock`, `product_url`, `last_seen_at`.
- `price_history` — هر تغییر قیمت یک ردیف (ورودی نمودار قیمت و هشدارها).
- `match_candidates` — پیشنهاد تطبیق `source_item → product` با `score` و `status` (`pending`/`approved`/`rejected`) برای تایید انسانی تیم داده.
- `redirect_clicks` — لاگ کلیک خرید، **پارتیشن‌بندی ماهانه بر اساس زمان**.
- `search_queries` — لاگ جست‌وجو برای گزارش «جست‌وجوهای پرتکرار» و کشف کالاهای غایب.
- `users`, `otp_codes`, `favorites`, `price_alerts`.
- `feature_flags` — از جمله فلگ «خرید مستقیم» فاز ۲ به تفکیک داروخانه.
- `audit_logs` — هر تغییر تیم داده و مدیر (چه کسی، چه چیزی، قبل/بعد).

قواعد: مبلغ‌ها `BIGINT` بر حسب **ریال** (هرگز float)، همه زمان‌ها `TIMESTAMPTZ` در UTC، حذف نرم (`deleted_at`) برای موجودیت‌های محتوایی، مایگریشن‌ها فقط forward و بدون ویرایش فایل مرج‌شده.

---

## ۵. خط داده (Ingestion)

```
Scheduler → Job (per source) → Fetch (API/Crawl) → Normalize → Match → Upsert Offer → PriceHistory → Reindex Search
```

- هر منبع در Job مستقل و ایزوله اجرا می‌شود؛ خطای یک داروخانه بقیه را متوقف نمی‌کند.
- `Fetcher` یک اینترفیس است: `Fetch(ctx, source) ([]RawItem, error)`. هر داروخانه یک پیاده‌سازی. افزودن داروخانه جدید = یک فایل جدید + یک ردیف در `data_sources`، بدون دست‌زدن به هسته.
- کرال با احترام: rate-limit به‌تفکیک دامنه، `robots.txt`، User-Agent شفاف، backoff نمایی، timeout مشخص.
- نرمال‌سازی فارسی اجباری قبل از تطبیق: تبدیل `ي/ك` عربی، یکسان‌سازی نیم‌فاصله، ارقام فارسی/عربی → لاتین، حذف نویسه‌های صفرعرض.
- تطبیق چندمرحله‌ای: کد یکتا (GTIN/IRC) → تطبیق دقیق نام نرمال‌شده → شباهت trigram با آستانه. زیر آستانه → `match_candidates` برای تایید دستی، **نه** ادغام خودکار.
- ایمنی قیمت: جهش غیرعادی (مثلاً بیش از ±۷۰٪) offer را `suspicious` می‌کند و به صف بازبینی می‌فرستد، نه مستقیم به سایت.
- هر اجرا یک `sync_run` با شماره‌ی آیتم موفق/ناموفق ثبت می‌کند؛ همین ورودی صفحه «پایش سلامت همگام‌سازی» است.

---

## ۶. لاگ و مشاهده‌پذیری

خروجی همه‌ی لاگ‌ها **JSON روی stdout** با `slog` است (بدون نوشتن فایل؛ جمع‌آوری وظیفه‌ی محیط اجراست). فیلدهای پایه در همه‌ی رکوردها:

```json
{"time":"...","level":"INFO","msg":"offer.updated","service":"worker",
 "request_id":"...","trace_id":"...","actor_id":"...",
 "source_id":42,"product_id":1337,"duration_ms":18}
```

قواعد:

- `msg` نام رویداد در قالب `domain.action` است (`search.query`, `redirect.click`, `crawl.failed`, `match.auto_approved`) تا لاگ‌ها قابل تجمیع و آلارم‌گذاری باشند.
- `request_id` در middleware ساخته می‌شود، در `context` جریان دارد و در هدر پاسخ برمی‌گردد.
- سطوح: `DEBUG` فقط توسعه، `INFO` رویدادهای کسب‌وکار، `WARN` خطای قابل بازیابی (retry کرال)، `ERROR` نیازمند دخالت انسان.
- **هرگز** لاگ‌کردن شماره موبایل کامل، کد OTP، توکن یا هدر `Authorization`. شماره موبایل ماسک می‌شود (`0912***4567`).
- لاگ‌های کسب‌وکاری که گزارش می‌شوند (کلیک ری‌دایرکت، جست‌وجو) **علاوه بر** لاگ متنی در جدول Postgres هم ثبت می‌شوند؛ گزارش‌ها از جدول می‌آیند نه از فایل لاگ.
- ثبت کلیک ری‌دایرکت غیرهم‌زمان است و هرگز ری‌دایرکت کاربر را کند یا مسدود نمی‌کند.
- `/healthz` (سرزنده) و `/readyz` (اتصال DB و صف) برای هر سرویس اجباری است.

---

## ۷. API و Frontend

- REST با پیشوند نسخه: `/api/v1/...`. نام‌گذاری منابع جمع و kebab-case.
- خطا با ساختار واحد: `{"error":{"code":"product_not_found","message":"...","request_id":"..."}}`.
- سه گروه مسیر: عمومی (`/api/v1/...`)، پنل داده (`/api/v1/ops/...`)، مدیر (`/api/v1/admin/...`).
- احراز هویت: OTP پیامکی برای کاربران، سشن با کوکی `HttpOnly` + `SameSite=Lax`. نقش‌ها: `guest`, `user`, `data_ops`, `super_admin`.
- صفحات محصول و دسته‌بندی با ISR رندر می‌شوند و `JSON-LD` نوع `Product` + `AggregateOffer` دارند؛ `sitemap.xml` از دیتابیس تولید می‌شود.
- کل UI **RTL** است؛ اعداد قیمت با جداکننده هزارگان فارسی نمایش داده می‌شوند اما در ورودی‌ها لاتین ذخیره می‌شوند.
- بودجه عملکرد: LCP زیر ۲.۵ ثانیه روی 4G، JS اولیه هر صفحه زیر ۱۵۰KB گزیپ‌شده، پاسخ جست‌وجو زیر ۲۰۰ms (p95).
- لینک خرید از مسیر `/go/{offer_id}` عبور می‌کند: ثبت کلیک، سپس `302` به `product_url` با `rel="nofollow sponsored"`.

---

## ۸. پالت رنگ پیشنهادی

حس هدف: **اعتماد پزشکی + شفافیت قیمت**. سبز-آبی به‌عنوان رنگ اصلی (سلامت و آرامش، پرهیز از سبز تند تبلیغاتی)، و یک نارنجی گرم فقط و فقط برای دکمه خرید تا CTA در میان جدول قیمت‌ها گم نشود.

| نقش | Hex | کاربرد |
|---|---|---|
| Primary | `#0D9488` | برند، هدر، لینک فعال، دکمه‌های اصلی |
| Primary Dark | `#0F766E` | حالت hover و متن روی سطح روشن |
| Primary Soft | `#CCFBF1` | پس‌زمینه بج و حالت انتخاب‌شده |
| Accent / CTA | `#EA580C` | فقط دکمه «خرید از داروخانه» |
| Best Price | `#15803D` | نشان «کم‌ترین قیمت» و قیمت برنده |
| Success / موجود | `#16A34A` | وضعیت موجودی |
| Warning | `#D97706` | داده کهنه، هشدار سلامت سینک |
| Danger / ناموجود | `#DC2626` | خطا، ناموجود، افت اعتبار منبع |
| Info | `#2563EB` | راهنما و اطلاعیه |
| Text Primary | `#0F172A` | متن اصلی |
| Text Muted | `#64748B` | متن ثانویه و برچسب |
| Border | `#E2E8F0` | خط جداکننده و حاشیه کارت |
| Surface | `#FFFFFF` | کارت و جدول |
| Background | `#F8FAFC` | پس‌زمینه صفحه |

قواعد رنگ: رنگ به‌تنهایی حامل معنا نباشد (موجود/ناموجود همیشه متن یا آیکن هم داشته باشد)، کنتراست حداقل AA (۴.۵:۱) برای متن، نارنجی جز CTA خرید جای دیگری استفاده نشود، و همه‌ی رنگ‌ها به‌صورت CSS variable در `styles/theme.css` تعریف شوند تا افزودن حالت تاریک در آینده بدون تغییر کامپوننت‌ها ممکن باشد.

---

## ۹. فیچرها به تفکیک نقش

### الف. کاربر مهمان (بدون ثبت‌نام)
- جست‌وجوی کالا بر اساس نام، برند یا ماده مؤثره + مرور دسته‌بندی‌ها.
- صفحه کالا با **فهرست قیمت همه داروخانه‌ها** (هسته محصول): تصویر، مشخصات، قیمت، موجودی، زمان آخرین به‌روزرسانی.
- فیلتر و مرتب‌سازی: قیمت، برند، داروخانه، فقط موجودها، ارزان‌ترین/گران‌ترین.
- مقایسه چند کالای هم‌رده در جدول.
- کلیک «خرید در [نام داروخانه]» → ثبت لاگ کلیک → ری‌دایرکت به سایت داروخانه. **بدون سبد خرید و پرداخت.**
- ثبت‌نام سریع با OTP، فقط زمانی که به علاقه‌مندی یا هشدار قیمت نیاز دارد.

### ب. کاربر ثبت‌نام‌کرده (اختیاری، برای شخصی‌سازی)
- ورود با OTP.
- مدیریت لیست علاقه‌مندی‌ها (Wishlist).
- هشدار کاهش قیمت یا موجود شدن مجدد کالا.
- تاریخچه جست‌وجو (nice-to-have).

### ج. تیم محتوا و داده (Data Ops)
> تأمین‌کننده، فروشنده، پزشک و داروساز در این فاز فقط «منبع داده» هستند و نقش کاربری مستقل ندارند.

- تعریف و مدیریت منبع داده هر داروخانه (اتصال API یا کرالر اختصاصی).
- تطبیق و یکپارچه‌سازی کالاهای مشابه چند منبع زیر یک کالای واحد (Product Matching) با صف تایید دستی.
- پایش سلامت همگام‌سازی: خطای کرال، قطعی API، داده کهنه، به‌روزرسانی قیمت/موجودی.
- مدیریت دسته‌بندی‌ها و برندها.
- تایید یا مخفی‌کردن کالاهای دارای اطلاعات نادرست/ناقص.
- گزارش کلیک‌های ری‌دایرکت به تفکیک داروخانه.

### د. مدیر کل (Super Admin)
- مدیریت کاربران داخلی و سطح دسترسی تیم محتوا.
- تنظیمات عمومی سایت: دسته‌بندی، بنر، SEO.
- داشبورد آمار کلی: ترافیک، جست‌وجوهای پرتکرار، نرخ کلیک ری‌دایرکت.
- فعال‌سازی Feature Flag «خرید مستقیم» برای یک داروخانه منتخب (فاز دوم).

### تمایز رقابتی فاز اول
جمع‌آوری قیمت از چند داروخانه (API یا کرال) + مقایسه صادقانه قیمت + ری‌دایرکت مستقیم، **بدون واسطه شدن در فروش**. هر فیچری که این سه را تقویت نکند، در فاز اول خارج از دامنه است.

### آماده‌سازی فاز دوم (فقط طراحی، بدون پیاده‌سازی)
`feature_flags` به تفکیک داروخانه، جدایی `offer` از `product` (پیش‌نیاز سفارش)، و مرز ماژول‌ها به‌گونه‌ای که افزودن `ordering` نیازی به بازنویسی `catalog`/`pricing` نداشته باشد.

---

## ۱۰. قواعد کار برای Agent

1. تصمیم استک را بدون بحث در این فایل تغییر نده؛ افزودن هر وابستگی جدید باید در PR توجیه شود و اگر با «حداقل وابستگی» در تضاد است، رد است.
2. منطق کسب‌وکار فقط در `service.go`. handler نازک، repository بی‌منطق.
3. هر SQL از `db/queries` و sqlc می‌آید؛ کوئری رشته‌ای دستی در handler ممنوع.
4. هر `error` یا wrap شود یا log — نه هر دو، نه هیچ‌کدام. `err` هرگز خورده نشود.
5. `context.Context` در همه‌ی مسیرهای I/O جریان داشته باشد و timeout مشخص داشته باشد.
6. تست: unit برای `service` (با repository جعلی) و integration برای هر `Fetcher` روی HTML/JSON ذخیره‌شده در `testdata`. تطبیق کالا و محاسبه قیمت بدون تست مرج نمی‌شود.
7. کد فارسی‌نویس نشود: نام متغیر، تابع، جدول و برنچ انگلیسی؛ فقط متن UI و محتوا فارسی.
8. تغییر شکل داده حتماً با فایل مایگریشن جدید؛ ویرایش مایگریشن قدیمی ممنوع.
9. هیچ راز و کلیدی در ریپو نباشد؛ همه از env و `deploy/.env.example` مستند شوند.
10. اگر فیچری در `features.md` نیست، پیش از ساختن آن سؤال بپرس.
